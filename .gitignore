__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# DuckDB is fully derived from registry/ + chckd-context/ — never committed
data/*.duckdb
data/*.duckdb.wal

logs/

.DS_Store

# API keys — copy .env.example to .env and fill in, never commit the real file
.env

chckd-context/*

# Claude Code's local tool/session state — not project content
.claude/
